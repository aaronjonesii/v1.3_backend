# djangoAPI_nginx.conf

# the upstream component nginx needs to connect to
# upstream django {
#     # server unix:///tmp/uwsgi.sock;
#     # server unix:/home/jonesaaron1996_gmail_com/venv/api.anonsys.tech/djangoAPI.sock; # for a file socket
#     server 127.0.0.1:8001; # for a web port socket (we'll use this first)
# }

# configuration of the server
server {
    listen      80;
    server_name api.anonsys.tech;
    access_log /var/log/access.log;
    error_log /var/log/error.log;
    charset     utf-8;

    # max upload size
    client_max_body_size 75M;   # adjust to taste

    location /media  {
        alias /srv/www/pyenv/api.anonsys.tech/media;
    }

    location /static {
        alias /srv/www/pyenv/api.anonsys.tech/staticfiles;
    }

    location / {
        uwsgi_pass  unix:/tmp/uwsgi/uwsgi.sock;
        include     /etc/nginx/uwsgi_params;
    }
}
